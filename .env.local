# Local PostgreSQL Database Configuration
# Replace these values with your actual local PostgreSQL settings

DATABASE_URL=postgresql://postgres:password@localhost:5432/maharashtra_water_db
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=password
PGDATABASE=maharashtra_water_db

# Application Configuration
NODE_ENV=development
SESSION_SECRET=maharashtra-water-dashboard-local-secret

# Optional: OpenAI API Key (if needed for features)
# OPENAI_API_KEY=your_openai_api_key_here